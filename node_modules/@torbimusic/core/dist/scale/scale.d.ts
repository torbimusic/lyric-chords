import { Note } from '../note';
import { Chord } from '../chord';
import { Interval } from '../interval/interval';
export declare class Scale {
    protected _tonic: Note;
    protected _grades: Interval[];
    protected _gradesFromTonic: Interval[];
    protected _notes: Note[];
    protected _triads: Chord[];
    protected constructor(_tonic: Note, _grades: Interval[]);
    readonly tonic: Note;
    readonly grades: Interval[];
    readonly gradesFromTonic: Interval[];
    readonly notes: Note[];
    note(grade: number): Note;
    readonly triads: Chord[];
    triad(grade: number): Chord;
    thirdOf(note: Note): Note;
    fifthOf(note: Note): Note;
    gradeOf(note: Note, grade: number): Note;
}

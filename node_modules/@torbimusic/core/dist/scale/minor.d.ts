import { Scale } from "./scale";
import { Note } from "../note";
import { Interval } from "../interval/interval";
export declare class MinorScale extends Scale {
    protected _tonic: Note;
    private static TONALITIES;
    private static _grades;
    private constructor();
    traspose(interval: Interval): Scale;
    static from(tonic: Note): MinorScale;
    private static lazyCreation;
}

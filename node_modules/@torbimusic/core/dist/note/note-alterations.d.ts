import { NoteAlteration } from "../constants";
export declare class NoteAlterations {
    static parse(alterationString: string): NoteAlteration;
    static toString(alteration: NoteAlteration): string;
    static value(alteration: NoteAlteration): number;
}

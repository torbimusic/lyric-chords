import { ChordQuality } from '../../constants/note-qualities';
import { Note } from '../../note/note';
export declare class BaseChord {
    note: Note;
    quality: ChordQuality;
    constructor(note: Note, quality: ChordQuality);
    static clone(baseChord: BaseChord): BaseChord;
    toString(): string;
    toStringNaturalNote(): string;
    equals(chord: BaseChord): boolean;
    private qualityToString;
}
